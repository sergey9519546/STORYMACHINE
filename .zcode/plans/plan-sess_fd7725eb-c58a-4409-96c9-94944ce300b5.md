Full P0+P1+P2 remediation of the STORYMACHINE frontend. Targeted, in-place fixes — no new primitive library. Each phase ends with `npm run lint && npm run build && npm test` green, plus a re-run of `output/playwright/whole_site_qa.py` against the running app to confirm clipping/console counts drop. All evidence references are from the QA run already captured in `output/playwright/whole-site-qa.json`.

Decisions locked: (1) remove the broken Archivo/Caveat `@font-face` declarations and let the existing Inter/cursive fallbacks carry the load honestly; (2) rip out ScriptIDE's half-wired dark mode entirely.

---

## Phase 1 — P0: make every surface usable (responsive + overlay + fonts + dvh)

**1.1 Responsive Script IDE shell** (P0-1, browser-verified: 16–21 controls clip at 390px)
- `src/components/Sidebar.tsx:141` — collapse the fixed `w-80` sidebar below `md`: drawer/rail pattern with a hamburger toggle; full width on ≥md.
- `src/components/ScriptIDE.tsx:1029` — keep `overflow-hidden` on the desktop shell, but gate the three-column layout behind `md:`; on mobile render editor-only with sidebar/panels as overlays.
- `src/components/ScriptIDE.tsx:1347` — `w-[400px] shrink-0` right panel → overlay drawer on `< md`, docked column on `≥ md`.
- `src/components/editor/FountainEditor.tsx:65` — keep the ~816px screenplay page, but move its horizontal centering into a localized `overflow-x-auto` so the page scrolls inside the column instead of pushing siblings off-screen.

**1.2 Drawer sizing + Doctor header wrap** (P0-2, browser-verified: close buttons clip on mobile AND on 1440 desktop)
- `src/components/scriptide/ScriptDoctorPanel.tsx:2063`, `src/components/SlatePanel.tsx:408`, `src/components/DirectorPanel.tsx:509` — `h-screen`→`h-dvh`; already have `max-w-[94/96vw]` but the inner headers don't wrap. Add `flex-wrap` + gap to the Doctor header row (`ScriptDoctorPanel.tsx:2066`) so its 7 action buttons flow; verify close button stays in-view at 390px and 1440px.

**1.3 Story Machine overlay stack** (P0-3, browser-verified: 4 flow timeouts, backdrops intercept each other)
- `src/components/StoryMachine.tsx:1424-1631` — introduce a single `activeOverlay` state (one open at a time). Opening a tool closes any other; Escape closes the current one. Keeps existing per-panel components, just fixes the mount model.
- Add missing dialog semantics + Escape + labelled close to: `src/components/WhatIfPanel.tsx:714`, `src/components/InterviewPanel.tsx:329`, `src/components/RoomPanel.tsx:118` (also give its bare `x` button an `aria-label="Close"`), `src/components/RevisionPanel.tsx:539`. Match the pattern already used by ScriptDoctorPanel (`role="dialog" aria-modal="true" aria-labelledby=…` + the Escape `useEffect` at `ScriptDoctorPanel.tsx:2042`).

**1.4 Remove broken font declarations** (P0-4, console-verified: OTS decode error every load)
- `src/styles/design-system.css:47-49` — delete the two `@font-face` rules for Archivo and Caveat. The `--sm-font-body`/`--sm-font-hand` stacks at `:36-39` already fall back to Inter/system-ui and Segoe Script/cursive, so rendering is unchanged except the console errors disappear.
- `src/components/DesignPreview.tsx:31-33` — relabel "Archivo body" / "Caveat margin note" to reflect the actual fallback ("Inter body", "system handwriting margin note") so the preview stops advertising fonts that aren't loaded.

**1.5 `h-screen` → `dvh`** (P0-5)
- `src/components/ScriptIDE.tsx:49,59,1029`, `src/components/scriptide/ScriptDoctorPanel.tsx:2063`, `src/components/SlatePanel.tsx:408`, `src/components/DirectorPanel.tsx:509` — `h-screen` → `h-dvh`.
- `src/index.css:193` `.film-grain` `height:100vh` → `100dvh`; `src/components/DesignPreview.tsx:16` inline `minHeight:'100vh'` → `'100dvh'`.
- Leave `min-h-screen` on Start/ErrorBoundary (those are `min-h`, not the iOS-jump bug).

---

## Phase 2 — P1: coordination + accessibility

**2.1 Story Machine header regroup** (P1-3)
- `src/components/StoryMachine.tsx:608-867` — replace the 28-button color wall with 4–5 labeled groups (Simulate / Inspect / Analyze / Revise / Export), each `flex-wrap`, primary tools visible, secondary behind an overflow "More" menu. Reduce ~16 hue classes to 3 semantic ones (neutral / active / destructive). Replace emoji + Unicode glyphs (`🔬 🧬 ◈ ◎ ◷ ♥ ⟳ ✍ ✒`) with Lucide icons already in the import block; drop the per-button `bg-{color}-900` palette.

**2.2 Global reduced-motion** (P1-4)
- `src/index.css` — append `@media (prefers-reduced-motion: reduce){ *{animation-duration:.01ms!important; animation-iteration-count:1!important; transition-duration:.01ms!important; scroll-behavior:auto!important} }` plus kill `.brutal-shadow-hover` translate and `.animate-blink` under the same media query.
- `src/styles/design-system.css:101` — guard `.sm-pulse` with the media query.
- `src/App.tsx` — wrap `<App/>` in `<MotionConfig reducedMotion="user">` (motion is a dep). One import, one wrapper.

**2.3 Accessibility pass** (P1-5)
- Lift `StartScreen.tsx:48-49`'s `FOCUS_RING` to a tiny shared constant (`src/lib/cn.ts` already exists) and apply to Sidebar search (`Sidebar.tsx:174` — remove `outline-none` without replacement), icon-only close buttons, and Toolbar buttons.
- Add `aria-label` to the ~60 unlabeled inputs/selects/textareas — **scoped to highest-traffic**: Director, ScenarioBuilder, Sidebar, ResearchNotes, ScriptIDE collab bar. Full sweep flagged as follow-up.
- Replace `ScriptIDE.tsx:1122` `window.confirm` with a small in-app confirm modal (reuse the existing `actionModal` pattern at `ScriptIDE.tsx:1280-1339`).
- Label the bare `✕` close buttons: `ScriptIDE.tsx:1048`, `StoryMachine.tsx:581`, `SettingsPanel.tsx:627`, `RoomPanel.tsx:127`.
- Bump sub-44px icon-only buttons to `min-h-[44px] min-w-[44px]` (Toolbar's `py-1` cluster, analytical-panel close buttons).
- Fix contrast: nudge `--sm-ink-faint` from `#857c68` to ~`#6f6553` (≈4.6:1 on paper) in `design-system.css:25`; replace `text-ink/45` → `text-ink/60` in `StartScreen.tsx:362-363`; in the dark panels replace `#475569` body text with `#94a3b8`.

**2.4 Accent + state coordination, scoped** (P1-1, P1-6)
- In Start/wizard surfaces (`StoryConfigForm.tsx`, `ExplainerCard.tsx`, `StartScreen.tsx`) replace literal `#FF4444` with the `stamp` token (`text-stamp`/`bg-stamp`/`border-stamp`) and swap `brutal-border-thick` for the editorial `border border-ink/…` so the wizard stops visually breaking from its own frame.
- Standardize three high-traffic state idioms onto paper tokens: `App.tsx:9-13` lazy fallback, `ScriptIDE.tsx:1024` init, `ErrorBoundary.tsx:55-91` — shared `bg-paper text-ink` + `Loader2` spinner (not pulse-text).
- Leave the IDE drawers' brutalist language and the analytical panels' dark-slate language intact for this pass — migrating those 20+ panels is its own project. Flag explicitly in the final report as intentionally deferred.

**2.5 Black/white token remap** (P1-2) — **judgment call, scoped**
- The `index.css:135-136` remap (`black`→warm ink, `white`→cream) is intentional for the editorial vibe and reverting it would visually flip every `bg-black text-white` IDE surface. **Plan: leave the remap, but remove the conflicting literals** (`#111`, `#fff`, `#000`) in the panels I'm already touching (WhatIf, Interview, Room, Revision) so each surface stops mixing 2–3 different "blacks." Full de-duplication flagged as follow-up.

---

## Phase 3 — P2: cleanup

- **P2-1 wizard re-skin** — folded into 2.4.
- **P2-2 fonts** — point CodeMirror at `var(--font-courier)` in `FountainEditor.tsx:73` so the editor and title page share one screenplay face; either host Inter 700 or sweep IDE `font-bold`→`font-semibold` (prefer the latter — one regex pass over `src/components/scriptide/**`).
- **P2-3 DesignPreview overflow** — `DesignPreview.tsx:13,60` padding → `clamp()`; panel width → `min(364px, 100%)`.
- **P2-4 redesign.html** — add a header comment marking it as non-production documentation; not routed by the app, so no CSP risk to the real app. Leave as-is otherwise.
- **P2-5 dark mode rip-out** — remove `isDarkMode` state + persistence + `<html class=dark>` effect + `isDarkMode` from the server-load shape in `ScriptIDE.tsx:173-174,254-296,315-322`; drop `dark:` variants from the IDE shells I'm already editing. Surfaces become honestly light-only.
- **P2-6 dead code** — delete `src/components/DebuggerPanel.tsx` (no import/mount).

---

## Verification gates (after each phase)

1. `npm run lint` — tsc clean.
2. `npm run build` — vite build green, bundle size rechecked (ScriptIDE chunk is currently 538kB; not a target here, just baseline).
3. `npm test` — 9,523 pass, 0 fail maintained.
4. `PORT=4180 npm start` + `python output/playwright/whole_site_qa.py` — confirm: `screens_with_clipped_controls` drops to ≤2 (DesignPreview mobile + one known drawer edge), `flow_errors` → 0, `console` font-OTS warnings → 0.

## Out of scope (flagged for a future pass, not silently skipped)

- Full migration of the 20 analytical panels onto a shared dashboard shell.
- Full migration of the IDE Toolbar/drawers off `brutal-*` onto `sm-*`.
- Removing all 325 `#FF4444` / multi-hue accent literals app-wide.
- A real DialogShell/Panel/Button/StateNotice primitive library (user chose targeted fixes over this).
- Full dark-mode reintroduction (user chose to rip out, not repair).
- Vendoring real Archivo/Caveat .woff2 binaries (user chose honest fallback).