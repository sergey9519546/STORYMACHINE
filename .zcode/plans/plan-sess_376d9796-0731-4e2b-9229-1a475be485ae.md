Run the evals/scoring suite:

1. Execute `evals/scoring/runner/run-metamorphic.ts` — the 7-case metamorphic scoring harness that runs `runScriptDoctor` against `base.fountain` and asserts score directions
2. Run `evals/scoring/rubric/tssf-rubric.test.ts` — 15 TS-SF rubric tests
3. Run `evals/scoring/contracts/human-label-import.test.ts` — 24 human-label importer tests
4. Check if `golden/metamorphic-baseline.json` was written
5. Report results