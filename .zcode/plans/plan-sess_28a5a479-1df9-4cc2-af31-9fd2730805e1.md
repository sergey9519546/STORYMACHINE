## Plan: Fix generated tests + verify rulebook

### Current state
- **10,523 rules** across 14 pass files (3.27× the original 3,216) — all compile clean
- Generated test stubs have two bugs: wrong `buildPlainFountain` argument + missing imports
- Test fixtures are empty (`records: []`) — tests will compile but won't trigger rules (no-fire only)

### Steps

1. **Fix `buildPlainFountain(records)` → `buildPlainFountain(records.length)`** in all 14 test files  
   - The generated tests pass the records array, but the function expects a number (scene count)
   - Use a script or sed to fix all occurrences across all test files

2. **Add missing imports** to test files that don't have `buildPlainFountain` and `makeStructureState`  
   - `belief.test.ts` needs both imported from `./helpers.ts`
   - `structure.test.ts` needs `makeStructureState` added to existing import
   - Other test files may need similar fixes — check each one

3. **Regenerate rulebook docs** — run `npm run rulebook` to update `docs/rulebook/` with the new 10,523-rule count

4. **Run existing test suite** — `npm test` to verify nothing regressed (the existing ~3,841 tests should still pass; the new stubs will be no-fire tests that pass trivially with empty fixtures)

5. **Update ROADMAP.md** — update the rule count from 3,216 to ~10,523

### Files to edit
- `tests/passes/*.test.ts` (14 files) — fix imports + buildPlainFountain args
- `docs/rulebook/*` — regenerated by `npm run rulebook`
- `ROADMAP.md` — update rule count