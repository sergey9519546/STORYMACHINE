# Claims Register

Hand-maintained. Every user-facing empirical or guarantee claim the product
makes — anything that says the tool measures something, compares its output
to a human, promises determinism/reproducibility, promises privacy, or uses
an absolute word ("never", "always", "proven", "measured", "guaranteed") —
gets a row here, whether or not it currently holds up. **An unsupported claim
is listed as unsupported, not omitted.** This register exists because
`docs/audits/2026-09-02-retrospective/RETROSPECTIVE.md` finding #8 found that
`scripts/honesty-audit.mjs` caught banned *words*, not empirical *claims*: it
had no way to know that "reads your screenplay like a studio coverage
reader" was a promise about human agreement the product has never measured.

This file is enforced by `scripts/honesty-audit.mjs`'s claims lane (see its
"Claims-register lane" section) and exercised by
`tests/core/honesty-audit-claims.test.ts`:

- Every row with status `unsupported` or `retired` must **not** appear
  verbatim (whitespace-normalized) anywhere in the tracked tree outside this
  file and `docs/audits/**` (dated audit records — the retrospective is
  allowed to quote the problem it found).
- Every row with status `supported` must carry an evidence pointer that
  **exists on disk** (a `path`, or `path:line` — only the path is checked).
- A curated list of empirical-claim phrases (below) is banned anywhere in
  `src/**`, `README.md`, `ARCHITECTURE.md`, `NORTH_STAR.md`, `ROADMAP.md`,
  `docs/PATH_TO_EXCELLENCE.md`, `index.html` **unless** the exact sentence
  containing the phrase is registered here with status `supported` at that
  same file. The general `docs/**` exemption in `honesty-audit.mjs` does
  **not** cover these six named orientation docs for this lane — they are
  scanned like `src/**`.

## Evidence types

- **measured-in-repo** — a runnable test in this repo asserts the claim
  (cite the test file, and the `it(...)` block if it helps).
- **measured-owner-local** — measured by a documented owner-run process
  outside CI (e.g. `npm run measure-real` against the local real-script
  corpus) with a receipt committed to the repo.
- **human-agreement** — would require independent human readers agreeing
  with the tool's output. **This repo has none of these today** — see
  `ARCHITECTURE.md:305`'s own "Not yet proven by default CI: human agreement
  with scores" and the P1 program in `ROADMAP.md` §3, which is what would
  eventually produce this evidence.
- **none** — no evidence exists; the claim is not testable as stated, or
  nobody has run the measurement yet.

## Register

| # | Claim (verbatim) | Where it appears | Evidence type | Evidence pointer | Status |
|---|---|---|---|---|---|
| 1 | Reads your screenplay like a studio coverage reader and hands back a verdict, a craft score, and your next fix. | src/components/StartScreen.tsx:317 (original wording, replaced 2026-09-03) | human-agreement | NONE | retired |
| 2 | The page, read the way a studio reader reads it. | src/components/startscreen/SlugLineIntro.tsx:10 (original wording, replaced 2026-09-03) | human-agreement | NONE | retired |
| 3 | Runs deterministic coverage on your screenplay and hands back a verdict, a craft score, and your next fix. | src/components/StartScreen.tsx:317 | measured-in-repo | tests/routes/keyless-smoke.test.ts; tests/core/script-doctor.test.ts | supported |
| 4 | The page, read the way the engine reads it. | src/components/startscreen/SlugLineIntro.tsx:10 | measured-in-repo | tests/core/script-doctor.test.ts | supported |
| 5 | Keyless by default — your script stays in this deployment unless you turn on AI features yourself. | src/components/StartScreen.tsx:411 | measured-in-repo | tests/routes/keyless-smoke.test.ts | supported |
| 6 | Nothing — by default. Script Doctor, coverage export, What-If Lab, Writers' Room, and interview receipts are all deterministic and run entirely on this server; none of them call out anywhere. | src/components/PrivacyPage.tsx:129 | measured-in-repo | tests/routes/keyless-smoke.test.ts | supported |
| 7 | Neither store is sent anywhere by itself; both live only on this device, in this browser. | src/components/PrivacyPage.tsx:89 | measured-in-repo | ARCHITECTURE.md:267 (§8 Security boundaries) | supported |
| 8 | Inside the editor, open Settings … Delete Everything … asks this server to permanently delete this session's saved data. | src/components/PrivacyPage.tsx:153 | measured-in-repo | tests/routes/session-delete.test.ts | supported |
| 9 | Deterministic · reproducible · no LLM judge | src/components/scriptide/ScriptDoctorPanel.tsx:3443 | measured-in-repo | tests/core/script-doctor.test.ts (lines 1133, 1528) | supported |
| 10 | Story Machine — deterministic analysis, independently verifiable | src/components/VerifyReport.tsx:351 | measured-in-repo | tests/routes/export-verify.test.ts:240; tests/core/coverage-html.test.ts:354 | supported |
| 11 | Deterministic ranking — same slate, same order, every time. | src/components/SlatePanel.tsx:582 | measured-in-repo | tests/routes/export-producer.test.ts:96 | supported |
| 12 | The deterministic engine placed this draft in its top verdict tier — a measurement, not a human-reader endorsement. | src/components/SlatePanel.tsx:122 | measured-in-repo | tests/routes/export-producer.test.ts:96 | supported |
| 13 | Twelve critics debate the current story state — deterministic, no AI key required. | src/components/RoomPanel.tsx:159 | measured-in-repo | tests/routes/nvm-whatif-room.test.ts:185 | supported |
| 14 | Interventions are computed on the story's causal model — deterministic, no AI key needed. | src/components/WhatIfPanel.tsx:840 | measured-in-repo | tests/routes/nvm-whatif-room.test.ts:97 | supported |
| 15 | Deterministic core — Always available | src/components/SettingsPanel.tsx:854 | measured-in-repo | tests/routes/keyless-smoke.test.ts | supported |
| 16 | Story axes are deterministic engine config — no AI key required. | src/components/SettingsPanel.tsx:499 | measured-in-repo | tests/routes/keyless-smoke.test.ts | supported |
| 17 | Protection is a prompt-level request to the rewriter, not a hard guarantee — this check compares … | src/components/RevisionPanel.tsx:781 | measured-in-repo | src/components/RevisionPanel.tsx:94 (the FNV-1a hash check this sentence describes) | supported |
| 18 | The deterministic surface (Script Doctor, coverage export, What-If Lab, Writers' Room, interview receipts) works keyless. | README.md:23 | measured-in-repo | tests/routes/keyless-smoke.test.ts | supported |
| 19 | A deterministic core inside a generative shell … Pure, keyless, reproducible. No LLM, no wall clock, no `Math.random()` on the diagnostic path. | ARCHITECTURE.md §1 (Product architecture) | measured-in-repo | tests/core/script-doctor.test.ts:1133,1528; tests/routes/keyless-smoke.test.ts | supported |
| 20 | Not yet proven by default CI: human agreement with scores, public multi-tenant security. | ARCHITECTURE.md:305 (§9 Testing topology) | human-agreement | ARCHITECTURE.md:305 (self-evidencing — the claim is the disclosure itself, that this evidence does not yet exist) | supported |
| 21 | AUC-24 >= 0.622 … last measured 0.731. | CLAUDE.md "Standing task" section; ROADMAP.md §3 | measured-owner-local | tests/core/real-script-corpus.test.ts (env-gated on `REAL_SCRIPT_CORPUS_DIR`, skipped in CI); `docs/p1-benchmark/MEASUREMENT_RECEIPTS.md` | supported |
| 22 | The rulebook's 3,217 pass-scoped constants are a maintained conceptual set, not a quality claim; the weighted-rule channel contributes AUC ~0.076 to discrimination while scene-count scarcity carries AUC ~0.938. | CLAUDE.md "Standing task" section; NORTH_STAR.md | measured-in-repo | server/nvm/analyze/doctor.ts:1892-1898 | supported |
| 23 | Live Notes squiggles — Always available (deterministic) | src/components/SettingsPanel.tsx:853 | measured-in-repo | tests/routes/keyless-smoke.test.ts:92 (`/api/scriptide/diagnose` on a keyless server); tests/core/generative-surface-labs-gate.test.ts | supported |
| 24 | Cost: free to self-host — no account, no subscription, no per-report fee. The deterministic analysis surface … needs no API key at all; a key only unlocks optional generation features. | README.md (top section) | measured-in-repo | tests/routes/keyless-smoke.test.ts (no-API-key operation); package.json (no billing/payment/account dependency anywhere in the tree) | supported |
| 25 | There is no server-side backup of this by default. | src/components/PrivacyPage.tsx (wording removed 2026-09-04) | measured-in-repo | NONE — POST /api/reset takes an automatic verified SQLite copy of the whole session into data/backups/session-resets/, so "no server-side backup" was false for any writer who had ever used Reset | retired |
| 26 | On the server that same click also removes the reset-recovery copies described above, closes and drops any share-link rooms this session created, and clears the analysis this server had cached in memory for your script. | src/components/PrivacyPage.tsx (Deleting it) | measured-in-repo | tests/core/session-delete-reset-backups.test.ts; tests/routes/session-delete-memory-stores.test.ts; tests/collab/room-purge.test.ts; scripts/verify-e4-local-safety-net.mjs §4 | supported |
| 27 | Those copies are capped (five per session, a week each) and Delete Everything removes them along with the live file. | src/components/PrivacyPage.tsx (What the server stores) | measured-in-repo | tests/core/session-delete-reset-backups.test.ts; server/lib/session-store.ts (SESSION_RESET_BACKUP_KEEP / SESSION_RESET_BACKUP_TTL_HOURS defaults) | supported |
| 28 | If you open a share link, the server also holds the shared copy of that document in memory for as long as the room lives (a day at most, sooner if the server restarts). | src/components/PrivacyPage.tsx (What the server stores) | measured-in-repo | tests/collab/room-purge.test.ts; server/lib/collab-rooms.ts (COLLAB_ROOM_TTL_MS default 24h); server/collab/yjs-server.ts (in-memory only, no persistence) | supported |
| 29 | no route logs your script text, your title, or a character's name. A test asserts that by running the whole surface with a marker string and failing if it ever appears in the process output. | src/components/PrivacyPage.tsx (What leaves this deployment) | measured-in-repo | tests/routes/no-writer-content-in-logs.test.ts | supported |
| 30 | Each snapshot also carries the score it had when you took it — health, verdict, scene count, and when it was analyzed … deleted with it. | src/components/PrivacyPage.tsx (What stays in this browser) | measured-in-repo | server/lib/validation.ts (SnapshotSchema's health/verdict/sceneCount/analyzedAt); scripts/verify-e4-local-safety-net.mjs §4 (two marked snapshots saved, then byte-searched for after the delete) | supported |
| 31 | What it cannot reach, stated plainly: files you exported yourself; copies a collaborator you shared a link with has already taken; and, if your operator ran the backup script by hand, the archive they made. | src/components/PrivacyPage.tsx (Deleting it) | measured-in-repo | scripts/backup-sessions.ts (operator-run, writes under BACKUP_DIR — a root destroySession deliberately does not touch); server/lib/session-store.ts (destroySession's scope) | supported |
| 32 | Rank among your drafts: … of … runs and saved drafts of this script (by health) | src/components/scriptide/ScriptDoctorPanel.tsx (DraftRankLine, via src/lib/draft-rank-copy.ts's draftRankDenominatorLabel() — 2026-09-05 review fix: the noun moved from a hand-written "(by health, your own saved drafts of this script)" tail to the shared denominator, ahead of "(by health)") | measured-in-repo | tests/core/snapshot-trend.test.ts (computeDraftRank); tests/core/percentile-copy-consistency.test.ts ("draft-rank-copy.ts — no surface re-implements it"); tests/core/draft-rank-copy-consistency.test.ts (the one-sentence pins). SCOPE FIX (2026-09-05): "of this script" was false until then — the denominator was the whole global `sm_doctor_history_v1` array plus the editor's snapshots, so a writer looking at one script was ranked against another script's runs and against the built-in sample. The union is now scoped to the analyzed document's own script key (src/lib/doctor-history-identity.ts), and the sample is excluded outright; tests/core/doctor-history-identity.test.ts ("reproduces the hunter's states 2 and 3 honestly once scoped"); scripts/verify-p2-p3-surfaces.mjs (P3-provenance) | supported |
| 33 | First saved draft — rank among your drafts appears after your next run or save | src/components/scriptide/ScriptDoctorPanel.tsx (DraftRankLine, via src/lib/draft-rank-copy.ts's draftRankNextOpportunityLabel() — 2026-09-05 review fix: "your next run or save" replaces "your next save", since a rank can also appear after simply running the doctor again, not only after explicitly saving a Version) | measured-in-repo | tests/core/snapshot-trend.test.ts (computeDraftRank: "with no saved snapshots at all, the current draft is \"1st of 1\""). Qualification (audit, 2026-09-04 evening): the test proves the return value, not the promise — when saved snapshots exist but none carries a health value the same line renders and the next save does not produce a rank; that state renders its own distinct copy (DraftRankLine's `rank === null` branch, "N saved drafts have no score yet — run the doctor before saving to rank them"), not registered here as it is not currently exported to the letter or the HTML surfaces. | supported (qualified) |
| 34 | Among your own runs and saved drafts of this script, this one ranks … by health — a comparison to your own history, not to the reference set above or to any other writer’s work. | server/lib/coverage-letter.ts (buildCaveats, draftRank branch, via src/lib/draft-rank-copy.ts's draftRankDenominatorLabel() — 2026-09-05 review fix, same denominator drift as row 32) | measured-in-repo | tests/core/coverage-letter.test.ts ("the denominator noun phrase is draftRankDenominatorLabel() verbatim"); tests/routes/export-coverage-letter.test.ts | supported |
| 35 | This is your first saved draft of this script — a rank among your own drafts will appear after your next run or save. | server/lib/coverage-letter.ts (buildCaveats, draftRank branch, via src/lib/draft-rank-copy.ts's draftRankNextOpportunityLabel() — 2026-09-05 review fix, same "next run or save" wording as row 33) | measured-in-repo | tests/core/coverage-letter.test.ts ("renders \"first saved draft\" copy, not a fabricated rank, when of <= 1") | supported |
| 36 | Read from the shape of the document — word, line, sentence, turn and speaker counts — with no word list involved. Diagnostic only: nothing here feeds health, grade, verdict, or any priority above. | src/components/scriptide/ScriptDoctorPanel.tsx (ShapeRhythmSection, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts; server/nvm/analyze/structural-signals.ts (module header, no lexicon/rule) | supported |
| 37 | Mean scene-to-scene change in the dialogue/action word mix — descriptive only, not part of the score. | src/components/scriptide/ScriptDoctorPanel.tsx (ShapeRhythmSection, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts | supported |
| 38 | Sentence-length variation across the draft's action lines — descriptive only, not part of the score. | src/components/scriptide/ScriptDoctorPanel.tsx (ShapeRhythmSection, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts | supported |
| 39 | Shape and rhythm: … the mean scene-to-scene change in the dialogue/action word mix is …, and the sentence-length variation across the draft's action lines is … Both are descriptive only … no part of the score, grade, or verdict above is derived from them. | server/lib/coverage-letter.ts (buildCaveats, structuralSignals branch, 2026-09-04) | measured-in-repo | tests/core/coverage-letter.test.ts ("shape and rhythm caveat" describe block) | supported |
| 40 | Shape & rhythm (descriptive, not part of the score): Talk/action swing … → … · Action-prose variation … → … | src/components/scriptide/SnapshotManager.tsx (ShapeRhythmTrendLine, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts; tests/core/snapshot-trend.test.ts | supported |
| 41 | Shape & rhythm (descriptive, not part of the score): Talk/action swing … → … / Action-prose variation … → … | src/components/scriptide/ScriptDoctorPanel.tsx (FixReceiptCard structural-signals delta, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts; tests/routes/scriptide-fix.test.ts (both the generated and the writer-supplied receipt — the latter needs no key, so this strip is reachable on a keyless deploy, which lifts the reachability qualification this row carried, 2026-09-04); scripts/verify-p2-p3-surfaces.mjs (P2-generative, keyless server) | supported |
| 42 | Shape & rhythm (descriptive, not part of the score) · Talk/action swing … · Action-prose variation … | src/components/WhatIfPanel.tsx (DoctorReadout, 2026-09-04) | measured-in-repo | tests/core/shape-rhythm-panel-copy.test.ts ("WhatIfPanel — the Lab's Script Doctor readout" describe block); scripts/verify-p2-p3-surfaces.mjs (P2-whatif labelling assertion) | supported |
| 43 | Branches are story moves, not text — until they are compiled into a script there is nothing for the Script Doctor to read. "Score with Script Doctor" compiles each branch into a Fountain draft (deterministic, no AI key) and scores it. | src/components/WhatIfPanel.tsx (Lab results, pre-score empty state, 2026-09-04) | measured-in-repo | tests/routes/nvm-whatif-doctor.test.ts ("is byte-for-byte deterministic …"); server/nvm/whatif/materialize.ts (no LLM/randomUUID/wall-clock on the path); tests/routes/route-capabilities.test.ts (POST /api/nvm/whatif/doctor listed deterministic) | supported |
| 44 | The whole draft could not be analyzed, so no health, grade or verdict is shown. | src/components/WhatIfPanel.tsx (DoctorReadout withheld-score path, 2026-09-04) | measured-in-repo | tests/routes/nvm-whatif-doctor.test.ts ("withholds health and grade — and the delta — on a session with nothing to analyze, rather than inventing a score") | supported |
| 45 | This replaces the editor's draft with this branch's script. Your current draft is snapshotted first, so you can restore it from Versions. | src/components/WhatIfPanel.tsx (promote confirmation, 2026-09-04) | measured-in-repo | scripts/verify-p2-p3-surfaces.mjs (P2-whatif "Promote snapshots the PREVIOUS draft first (the undo path)" and "applies exactly ONCE") | supported |
| 46 | Rewrite the draft yourself in the editor, then measure it against this report. The Script Doctor re-reads both drafts and shows exactly what moved — no AI, no key needed. | src/components/scriptide/ScriptDoctorPanel.tsx (Verify my rewrite, 2026-09-04) | measured-in-repo | tests/routes/scriptide-fix.test.ts ("writer-supplied candidate: 200 with the full receipt, usedLLM:false, source:\"writer\"" — keyless test server, and a COUNTING provider spy asserted at zero invocations, so the guard fails if a model call is planted into the branch); scripts/verify-p2-p3-surfaces.mjs (P2-generative, keyless server, Labs OFF) | supported |
| 47 | Measured by the Script Doctor. No AI was used. | src/components/scriptide/ScriptDoctorPanel.tsx (FixReceiptCard, writer-supplied candidate, 2026-09-04) | measured-in-repo | tests/routes/scriptide-fix.test.ts (a counting provider spy is asserted at zero invocations across the writer request — mutation-checked: planting a swallowed fixAndVerify call into that branch turns the assertion red); server/routes/scriptide.ts (the candidateFountain branch returns before fixAndVerify is imported) | supported |
| 48 | Your draft is unchanged since this report — verifying now reports a zero delta. | src/components/scriptide/ScriptDoctorPanel.tsx (Verify my rewrite caption, 2026-09-04) | measured-in-repo | tests/routes/scriptide-fix.test.ts ("writer-supplied candidate: an identical candidate yields exactly zero deltas") | supported |
| 49 | Health percentile: … within a 20-sample, hand-authored synthetic reference set | server/lib/coverage-html.ts (buildHealthPercentileLine, 2026-09-04 honesty-audit matrix fix — second surface for the same reading src/components/scriptide/ScriptDoctorPanel.tsx already shows) | measured-in-repo | tests/core/coverage-html.test.ts ("renders the health-percentile line with the exact panel copy when healthPercentile is present") | supported |
| 50 | Rank among your drafts: … of … runs and saved drafts of this script (by health) | server/lib/coverage-html.ts (buildDraftRankLine, 2026-09-05 — third surface for row 32's claim; migrated to src/lib/draft-rank-copy.ts's shared helpers, closing a wording drift where this export still said "your own saved drafts of this script" after rows 32/34 had already moved to the shared denominator) | measured-in-repo | tests/core/coverage-html.test.ts ("renders the draft-rank line via the shared draftRankDenominatorLabel(), not the old stale copy"); tests/core/percentile-copy-consistency.test.ts ("draft-rank-copy.ts — no surface re-implements it"); tests/core/draft-rank-copy-consistency.test.ts (byte-identical to the panel line, for ranked/tied/unscored/first-draft) | supported |
| 51 | First saved draft — rank among your drafts appears after your next run or save | server/lib/coverage-html.ts (buildDraftRankLine, 2026-09-05 — third surface for row 33's claim; same migration as row 50) | measured-in-repo | tests/core/coverage-html.test.ts ("renders the \"first saved draft\" copy via the shared draftRankNextOpportunityLabel() when draftRank.of <= 1"); tests/core/draft-rank-copy-consistency.test.ts | supported |
| 52 | Ranks … of … by health among your saved drafts of this script / Only saved draft with a health score so far | src/components/scriptide/SnapshotManager.tsx (SnapshotPercentileAndRankLine, 2026-09-04 honesty-audit matrix fix — per-snapshot rank in the Versions list, reusing `computeDraftRank`, never a second ranking implementation; 2026-09-05 owner rule "one wording per concept" — the denominator noun now comes from src/lib/draft-rank-copy.ts's `draftRankDenominatorLabel('saved')`, the last draft-rank surface off the shared helpers, adding "of this script" to what was a bare "your saved drafts") | measured-in-repo | tests/core/snapshot-trend.test.ts (`snapshotDraftRanks` describe block, incl. "agrees exactly with computeDraftRank(others, thisHealth) called directly"); tests/core/draft-rank-copy-consistency.test.ts (the 'saved'-scope cases); tests/core/percentile-copy-consistency.test.ts ("draft-rank-copy.ts — no surface re-implements it"); scripts/verify-p2-p3-surfaces.mjs (P2-whatif "Ship -> Versions shows each snapshot's rank …") | supported |
| 53 | Shape & Rhythm column, titled "Descriptive only — not part of the score or this ranking" | server/lib/slate.ts (renderSlateHtml); src/components/SlatePanel.tsx (2026-09-04 honesty-audit matrix fix — same two structural-signal aggregates every other surface shows, added to the Slate table) | measured-in-repo | tests/routes/export-producer.test.ts (the "renders a Shape & Rhythm column" and "carries the two Shape & Rhythm aggregates" tests) | supported |
| 54 | top … of a 20-sample, hand-authored synthetic reference set | src/components/scriptide/SnapshotManager.tsx (`SnapshotPercentileAndRankLine`) AND src/components/WhatIfPanel.tsx (`DoctorReadout`, 2026-09-05) — both call the same `compactPercentileNote()`, src/lib/percentile-copy.ts (2026-09-04 review fix: the first version of SnapshotManager.tsx's line had silently dropped "hand-authored synthetic") | measured-in-repo | tests/core/percentile-copy-consistency.test.ts (the "compactPercentileNote() ALSO carries the hand-authored synthetic qualifier", "SnapshotManager.tsx renders the compact note via the shared compactPercentileNote()", and "WhatIfPanel.tsx's DoctorReadout renders the percentile beside health via the shared compactPercentileNote()" tests) | supported |
| 55 | Health ranks in the … percentile against a fixed, 20-sample, hand-authored synthetic reference set — not against other scripts you might send it, and not a market comparison. | server/lib/coverage-letter.ts (`buildCaveats`, healthPercentile branch — 2026-09-05 review fix: the previous version hardcoded a literal "th" suffix on every number, e.g. "82th" instead of "82nd", and had already dropped "synthetic"; now built from the shared `ordinal()`/`REFERENCE_SET_SIZE`/`REFERENCE_SET_LABEL`, src/lib/percentile-copy.ts) | measured-in-repo | tests/core/percentile-copy-consistency.test.ts (the two coverage-letter.ts percentile tests); tests/fixtures/coverage-letter/report1.expected.md | supported |
| 56 | Rank among your drafts: tied … of … runs and saved drafts of this script (by health) | src/components/scriptide/ScriptDoctorPanel.tsx (DraftRankLine, `draftRank.tied` branch, audit round 2, 2026-09-04); server/lib/coverage-letter.ts (buildCaveats, "ties for" wording, same round); server/lib/coverage-html.ts (buildDraftRankLine — new to this surface as of the 2026-09-05 migration that closed rows 32/50's wording drift; previously this export had no tied-aware wording at all, only a plain untied ordinal, so a genuine dead heat read as clean separation on the exported HTML specifically) | measured-in-repo | tests/core/coverage-letter.test.ts ("draftRank.tied renders \"ties for\" instead of \"ranks\""); tests/core/coverage-html.test.ts ("prefixes \"tied\" when opts.draftRank.tied is true"); tests/core/draft-rank-copy-consistency.test.ts | supported |
| 57 | … of … runs and saved drafts of this script are unranked (saved without a fresh diagnosis) — singular "1 of … runs and saved drafts of this script is unranked (saved without a fresh diagnosis)" when exactly one draft is unranked (`unrankedDraftsNote`'s own `unscored === 1 ? 'is' : 'are'` branch, `src/lib/draft-rank-copy.ts`) | src/components/scriptide/ScriptDoctorPanel.tsx (DraftRankLine's `unrankedNote`, review round 2, 2026-09-05); server/lib/coverage-letter.ts (buildCaveats, `unrankedDraftsNote` clause, same round); server/lib/coverage-html.ts (buildDraftRankLine — new to this surface as of the 2026-09-05 migration; the schema (`server/lib/validation.ts`'s DraftRankSchema) has validated `unscored` on the wire since it was added, but this export's own type annotations were narrower than the schema and silently discarded it before this migration, so the exported HTML never disclosed unranked drafts even when the panel and the letter did); src/components/scriptide/SnapshotManager.tsx (2026-09-05 follow-up, client-hunter B-12 — the 'saved'-scope badge had NO unranked note at all until draftRankSentence()) | measured-in-repo | tests/core/coverage-letter.test.ts ("draftRank.unscored > 0 (mixed ranked + unscored) adds the \"N of M ... are unranked\" clause"); tests/core/coverage-html.test.ts ("appends the unranked-drafts note when opts.draftRank.unscored > 0"); tests/core/draft-rank-copy-consistency.test.ts (the singular-"is" case, `unscored: 1`) | supported |
| 58 | The sample is not ranked against your drafts — it is a demo script, not your work. | src/components/scriptide/ScriptDoctorPanel.tsx (`DraftRankOrSampleNote`, 2026-09-05) — the line that replaces the rank on the built-in sample, which used to be ranked "among your drafts" one paragraph above the sentence withholding verification *because* it is not the writer's draft | measured-in-repo | tests/core/doctor-history-identity.test.ts ("renders an honest line for the sample instead of a rank or a silent gap"); scripts/smoke-p0-live-flow.mjs (golden path: the line renders and no "Rank among your drafts:" claim appears); scripts/verify-p2-p3-surfaces.mjs (P3-provenance) | supported |
| 59 | This report is the built-in sample script, not your draft. Replace the editor's text with your own draft and run the diagnosis again to verify a rewrite of it. | src/components/scriptide/ScriptDoctorPanel.tsx (`verifyBlockedReason`, threaded-sample branch, 2026-09-05) — the StartScreen entry point installs the sample into the EDITOR and threads its report in, so there is no upload chip to dismiss; the older "Dismiss the sample (✕ above)" sentence (row unchanged, panel-loaded branch) would name a control that is not on screen | measured-in-repo | scripts/smoke-p0-live-flow.mjs ("Verify my rewrite" withheld, with a reason, on the golden path); scripts/verify-p2-p3-surfaces.mjs (P3-provenance: visible, disabled, reason rendered); tests/core/doctor-history-identity.test.ts ("decides the Verify withhold from the REPORT's provenance before any upload state") | supported |
| 60 | Draft History: “N runs of this script · M elsewhere”, listed per script under the title each run was recorded with (“Built-in sample (not your draft)”, “Earlier drafts (recorded before runs were tracked per script)”) | src/components/scriptide/ScriptDoctorPanel.tsx (Draft History disclosure, 2026-09-05) — `sm_doctor_history_v1` is one global array; before this it was counted and listed as though every row belonged to whatever script was open | measured-in-repo | tests/core/doctor-history-identity.test.ts (`groupHistoryByScript` describe block: scoping, the count reconciliation, and "every entry stays listed — nothing is dropped by grouping"); src/lib/doctor-history-identity.ts | supported |

### Notes on rows 1–2 (the fix)

Rows 1–2 are the claims finding #8 named directly: an implicit promise that
the tool's read agrees with what a professional human script reader would
say, with `ARCHITECTURE.md:305` on record that human-agreement evidence does
not exist. Both were rewritten 2026-09-03 (rows 3–4 are what ships now) to
describe what the product actually does — run a deterministic analysis and
hand back a verdict, a craft score, and a next fix — without the human
comparison. Tone and information content (verdict, craft score, next fix)
are preserved; only the unsupported comparison is gone.

### Note on row 25 (the privacy claim that was false)

Row 25 is the one privacy sentence the 2026-09-04 store re-verification found
untrue rather than merely incomplete. `POST /api/reset` publishes a verified
SQLite online backup of the WHOLE session — script, title page, snapshots,
characters, research notes — into `data/backups/session-resets/<sessionId>/`
before it clears the simulation aggregate, and keeps up to five copies for a
week. So "there is no server-side backup of this by default" was false for any
writer who had ever pressed Reset, and worse, `destroySession()` did not remove
those copies, so they outlived "Delete Everything" by the rest of the retention
window. Reproduced in a live browser run: a 249KB `.db` under the backup root
still contained the marker string, the title page and both snapshots after the
deletion reported success.

Both halves were fixed rather than merely disclosed — `destroySession()` now
removes the directory, and the page describes the recovery copies plainly
instead of denying they exist (rows 26–27). Rows 28–29 cover the two stores
that were previously undescribed rather than misdescribed; row 31 states what
the control genuinely cannot reach, which is the part of an honest deletion
promise that is easiest to leave out.

### What this register deliberately does not do

It does not re-litigate `docs/p1-benchmark/DISCRIMINATION_BASELINE_2026-07-29.md`,
the AUC-24 ratchet, or the power-analysis gap — those are `ROADMAP.md` §3 /
P1 program concerns, tracked there and in
`docs/audits/2026-09-02-retrospective/RETROSPECTIVE.md` findings #7 and #10.
Rows 21–22 are included here only because their headline numbers are
user-facing-adjacent (they appear in `CLAUDE.md`, which every session reads)
and because `n-rules-claim`/`stale-count-*` in `honesty-audit.mjs` already
polices the numbers; this register additionally polices the *comparative
claim* riding on them ("provably discriminate", "AUC ~0.938") so a future
edit can't quietly detach the number from its measured, sourced context.
