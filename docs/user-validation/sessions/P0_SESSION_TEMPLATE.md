# P0 Session Template — SUPERSEDED

> **⚠️ DO NOT USE THIS TEMPLATE.** It is retained only to avoid breaking links.
>
> This earlier template conflicted with the authoritative
> `P0_OPERATING_KIT.md` in ways that could **invalidate sessions or leak
> personally identifying information** if followed:
>
> - It asks the **wrong core question**. The canonical question (ROADMAP §3,
>   `P0_OPERATING_KIT.md`) is verbatim: *does this make you want to run your
>   own draft — why or why not?* — not the paraphrased version this file used.
> - It had a **"Writer: [Name/Pseudonym]"** field. The kit forbids any name,
>   handle, or identifier in Git — use only an anonymous `P0-00N` id, and keep
>   no id-to-identity crosswalk in the repository.
> - It had a **"Script Shared: YES/NO"** field. The kit forbids collecting,
>   viewing, or running a participant's draft; sessions expose only the
>   existing sample report.
> - It used a **GREEN/YELLOW/RED** traffic-light classification. The kit uses
>   a five-way classification: Positive / Qualified / Negative / Ambiguous /
>   Invalid-excluded.

## Use the canonical template instead

Copy **`docs/user-validation/P0_SESSION_TEMPLATE.md`** (the parent-directory
file, ~15 KB) into this `sessions/` directory as `P0-00N.md` for each new
session. That template is reconciled with the operating kit and the ROADMAP
core question, and contains no PII or draft-collection fields.

See `P0_QUICK_START.md` for the cold-start run order, and
`P0_OPERATING_KIT.md` for the authoritative protocol (eligibility, consent,
exposure, evidence, privacy, classification).
