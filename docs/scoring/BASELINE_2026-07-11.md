# BASELINE (frozen) — 2026-07-11

The current engine measured before any redesign. Every future "50x better" claim
must beat THESE numbers on a held-out set. Do not tune against this file's
held-out portion. Machine-readable dump: `outputs/gap_analysis.json` (derived,
rights-safe; corpus text is local-only).

## Dataset
- **Reference (positive) corpus:** 45 produced animation features (Pixar/Disney/
  DreamWorks/Laika/Sony), reconstructed to Fountain (rights_tier=reference_only;
  no verbatim text stored). Scored through the real `runScriptDoctor`.
- **Degradation (negative) twins:** seeded shuffle-drop + act-swap of the same scripts (engine's own recipes).
- **Synthetic bands:** existing 20-sample controlled calibration corpus; discrimination pairs.

## Anchor — produced films through the full doctor (n=45)
| Metric | Value |
|---|---|
| health mean | **95.1** (min 88.3, median 96.3, max 98.9) |
| ≥ produced floor (80) | **45/45** |
| verdict | **RECOMMEND 45 / 45** (0 CONSIDER, 0 PASS) |
| dimension avg — structure-pacing | 97.0 |
| dimension avg — character | 93.8 |
| dimension avg — dialogue-voice | 98.3 |
| dimension avg — plot-logic | 91.6 |
| dimension avg — theme-originality | **100.0 (vacuous — theme fires 0)** |

Interpretation: the verdict layer is well-calibrated at the top (acclaimed films
rank high). Precision/coverage are the weak axes, not the verdict.

## Discrimination & degradation (engine's own ratchets)
| Metric | Baseline | Notes |
|---|---|---|
| Discrimination pairs ordered correctly | 6/6 | 5 hard-asserted in CI |
| Composite min-gap (composite-reviewer-scenario) | **2.2** | target ≥5.0 — open `todo` (§5.2) |
| Shuffle-drop AUC-24 | **0.672** | hard floor 0.622 |
| Act-swap AUC | **≈0.48** | ~chance — global-arc blind spot |

## False-positive baseline (rules firing on the MOST produced films)
Top over-firers on known-good films (candidates for tiering/de-dup; some are conversion artifacts — re-verify on clean corpus):
- `INTENTION_INVISIBLE` — 5,735 instances / 45 films (~127/film)
- `STACCATO_FRAGMENTATION` — 2,049
- lexical/punctuation cluster (EXCLAMATION/QUESTION/COLON_IN_ACTION, COMMA_SPLICE_OVERUSE, ACTION_* families) — fire on 45/45 films
- structural: NO_REVERSALS, CLIMAX_TOO_EARLY, FALSE_CLIMAX, CHEKHOV_GUN_UNFIRED, DENOUEMENT_OVERLONG — 45/45 films (1 each)
- `theme` pass — **0** issues on 0/45 films (blind spot: needs theme extraction)

## Test / quality gates (must never regress)
- Full suite: **9,032 tests, 0 fail, 1 todo** (off-OneDrive, dev deps).
- `tsc --noEmit`: clean. `vite build`: passes. Server boots keyless + serves.

## Human agreement / calibration
- **NONE.** No human-preference labels exist yet. Pairwise agreement, Kendall/
  Spearman, Brier, ECE, position/verbosity-bias = **unmeasured**. This is the
  #1 gap blocking any "agreement with expert judgment" claim (Phase G).

## What "50x better" must eventually show against this baseline
Held-out improvement in: human pairwise agreement (from 0 → measured), false-
positive rate on produced films (from the flood above → down), act-swap AUC
(0.48 → materially >0.5), composite min-gap (2.2 → ≥5.0), calibration ECE
(unmeasured → reported + low), with **no regression** in the 6/6 discrimination,
the 0.622 AUC floor, or the produced-floor (45/45 ≥80).
